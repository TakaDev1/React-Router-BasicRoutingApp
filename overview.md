# 課題

### 問題文

React Router v8 を使用して、Home ページと About ページを持つアプリを作成してください。

共通ナビゲーションを作り、ページ遷移できるようにしてください。

### 条件

1. `BrowserRouter`, `Routes`, `Route`, `Link` を使用する
2. ページは `Home` と `About` の2つ
3. ナビゲーションは TailwindCSS で `flex gap-4 p-4 bg-gray-100`
4. TypeScriptで作成する